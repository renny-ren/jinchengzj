<% reply = notification.target %>
<% if reply.blank? %>
  <div class="media-body">相关信息已删除</div>
<% else %>
  <div class="media-heading">
    在帖子 <%= link_to reply.topic.title, main_app.topic_path(reply.topic) %> 回复了：
  </div>
  <% if reply.present? %>
    <div class="media-content">
      <%= reply.body %>
    </div>
  <% end %>
<% end %>
