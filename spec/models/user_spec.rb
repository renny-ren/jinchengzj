require 'rails_helper'

describe User do
  it "is valid with username, password, captcha" do
    expect(true).to be true
  end
  it "is invalid without a username"
  it "is invalid without a password"
  it "is invalid without a captcha"  
end